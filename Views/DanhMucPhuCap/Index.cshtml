@model IEnumerable<DanhMucPhuCap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenPhuCap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoTien)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenPhuCap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoTien)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaPC">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaPC">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaPC">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
