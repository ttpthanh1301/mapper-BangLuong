@model IEnumerable<DanhMucKyLuatViewModels.DanhMucKyLuatViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenKyLuat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoTienPhat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenKyLuat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoTienPhat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaKL">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaKL">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaKL">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
