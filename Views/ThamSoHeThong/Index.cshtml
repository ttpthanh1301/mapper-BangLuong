@model IEnumerable<ThamSoHeThong>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenThamSo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GiaTri)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayApDung)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenThamSo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GiaTri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayApDung)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaTS">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaTS">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaTS">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
